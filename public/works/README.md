# works

drop your files here and they will be served from `/works/your-file.ext`.

supported (suggested): `.pdf`, `.mp3`, `.mp4`.

then register the work in `src/data/works.ts` with a slug, kind, themes, and
position on the sky (`x`, `y` as percentages 0-100).
